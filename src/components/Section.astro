---
interface Props { id: string; heading: string; }
const { id, heading } = Astro.props;
---
<section id={id} class="container" aria-labelledby={`${id}-heading`}>
  <h2 id={`${id}-heading`}>{heading}</h2>
  <slot />
</section>
